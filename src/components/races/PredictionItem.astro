---
interface Props {
  point?: number | null;
  prediction?: string | null;
  label: string;
}
const { point, prediction, label }: Props = Astro.props;
---

<div>
  <p class="text-2xl">
    {point ?? <span class="text-sm">—</span>}
  </p>

  <div class="mt-1">
    <p class="text-xs text-[#797979]">{label}</p>
    <p class="text-sm font-medium uppercase text-[#DADADA]">{prediction}</p>
  </div>
</div>
